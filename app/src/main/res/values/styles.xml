<resources>

    <style name="MainActivityTheme.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/michi_soft_pink</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>        <item name="postSplashScreenTheme">@style/Theme.MichiXO</item>
    </style>

    <style name="Theme.MichiXO" parent="@android:style/Theme.DeviceDefault" />

</resources>